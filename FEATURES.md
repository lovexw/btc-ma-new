# 功能特性详解

## 🎯 核心功能

### 1. 比特币价格走势图表

#### 主要特性
- **实时数据展示** - 读取CSV文件中的历史价格数据
- **渐变填充** - 价格曲线下方使用蓝色渐变填充，视觉效果优雅
- **平滑曲线** - 启用平滑算法，曲线更加流畅自然
- **阴影效果** - 曲线带有柔和阴影，增强立体感

#### 交互功能
- **缩放功能**
  - 鼠标滚轮缩放
  - 拖拽滑块调整时间范围
  - 双击重置视图
- **十字准星**
  - 鼠标悬停显示详细数据
  - 同时显示价格和各条均线值
  - 深色半透明背景，信息清晰可读

### 2. 多周期移动平均线

#### 支持的均线周期
| 周期 | 名称 | 用途说明 |
|------|------|----------|
| 50日 | MA50 | **短期趋势**<br>- 捕捉短期价格动向<br>- 适合短线交易参考<br>- 反应灵敏，波动较大 |
| 200日 | MA200 | **经典中期趋势**<br>- 最常用的技术指标之一<br>- 牛熊市分界线<br>- 重要支撑/压力位 |
| 350日 | MA350 | **中长期趋势**<br>- 介于中期和长期之间<br>- 平滑短期波动<br>- 趋势更加稳定 |
| 700日 | MA700 | **长期趋势**<br>- 约2年周期<br>- 识别长期牛熊市<br>- 重要战略参考位 |
| 1000日 | MA1000 | **超长期趋势**<br>- 约3年周期<br>- 极其稳定的趋势线<br>- 长期投资者关注 |
| 1400日 | MA1400 | **极长期趋势**<br>- 约4年周期<br>- 对应比特币减半周期<br>- 周期性分析重要指标 |

#### 均线控制
- **开关按钮** - 每条均线都可独立显示/隐藏
- **颜色区分** - 每条均线使用不同颜色，易于识别
- **图例显示** - 图表顶部显示当前激活的均线

### 3. 均线监控面板

#### 卡片信息
每个均线监控卡片包含以下信息：

1. **均线标识** (左上角)
   - 显示均线名称（MA50、MA200等）
   - 使用对应的主题色

2. **涨跌指标** (右上角)
   - 显示均线日变化率
   - 绿色↑表示上涨
   - 红色↓表示下跌

3. **当前数值** (中间大字)
   - 显示均线当前精确价格
   - 千分位格式化
   - 保留2位小数

4. **价格偏离度** (底部)
   - 显示当前价格与该均线的偏离百分比
   - 绿色表示价格在均线上方
   - 红色表示价格在均线下方

5. **位置提示** (最底部)
   - "价格在均线上方" - 表示多头
   - "价格在均线下方" - 表示空头

#### 视觉效果
- **渐变背景** - 每个卡片使用对应均线颜色的渐变背景
- **悬浮动画** - 鼠标悬停时卡片上升，阴影增强
- **响应式布局** - 自动适配不同屏幕尺寸
  - 大屏：6列
  - 中屏：3列
  - 小屏：2列
  - 手机：1列

### 4. 顶部数据概览

位于页面顶部卡片中，一眼掌握全局：

1. **最新价格** (橙色)
   - 最新数据日的比特币价格
   - 千分位格式化显示

2. **最新日期**
   - 显示最新数据的日期
   - 确认数据是否为最新

3. **AHR999 指数**
   - 当日 AHR999 指数值，按所处分区着色
   - 右侧带分区标签（绝佳抄底 / 抄底定投 / 正常定投 / 谨慎止盈）

4. **历史数据 / 数据起点**
   - 历史数据总天数与数据覆盖起点

### 5. 价格区间停留天数

以 1 万美元为区间宽度，统计比特币价格在 1 万美元以上各价格区间的停留天数（占比基于纳入统计的天数计算）：

- **双视图切换** - 右上角一键切换「条形 / 卡片」两种展示方式
- **横向条形图** - 条形长度为该区间天数占比，右侧显示天数与百分比
- **卡片视图** - 每个区间一张卡片，展示天数、占比与首次/最近出现的日期
- **当前区间高亮** - 现价所在区间以主题色高亮，并标注「现价在此」
- **悬停详情** - 鼠标悬停条形可查看该区间首次进入与最近一次出现的日期

## 🧭 AHR999 指数

集成九神（ahr999）《囤比特币》第十五章提出的囤币指标，辅助定投择时：

**计算公式**

```
AHR999 =（比特币价格 / 200日定投成本）×（比特币价格 / 指数增长估值）
```

- **200 日定投成本**：最近 200 日价格的几何平均（模拟每日等额定投的持仓成本）
- **指数增长估值**：币价与币龄的对数拟合 `价格 = 10^(5.84 × log10(币龄) − 17.01)`，币龄自创世区块日（2009-01-03）起算

**分区参考（四档）**

| AHR999 | 分区 | 参考策略 | 颜色 |
|--------|------|----------|------|
| ≤ 0.35 | 绝佳抄底 | 历史底部区域，适合加大投入 | 绿色 |
| 0.35 ~ 0.45 | 抄底定投 | 接近底部，定投 + 逢低加仓 | 浅绿 |
| 0.45 ~ 1.2 | 正常定投 | 常规定投区间 | 橙色 |
| > 1.2 | 谨慎止盈 | 错过最佳定投期，注意止盈 | 红色 |

**页面展示位置**

- 顶部数据概览：当日最新 AHR999 指数及分区标签
- 历年定投回测卡片：每年买入当日的 AHR999 指数及分区标签

## 🎯 历年定投回测

假设自 2016 年起，每年在今日的价格买入并持有至今，计算每一年买入的累计收益率：

- **回测卡片** - 每年一张卡片，突出显示买入价（大号加粗），附带买入当日的 AHR999 指数与分区、累计盈亏金额与收益率
- **红绿标识** - 盈利用绿色、亏损用红色，一目了然
- **自动基准** - 以最新数据日为基准日，同日无数据时自动向前寻找最近的数据

## 🎨 设计理念

### 色彩系统
- **主色调**：比特币橙（#FF9900）- 呼应主题、醒目
- **背景色**：浅灰色（#F6F7F9）- 柔和、舒适
- **卡片色**：白色 - 清晰、简洁
- **渐变顶条**：橙色到青色渐变 - 现代、友好

### 均线配色原则
- 每条均线使用独特且易区分的颜色
- 颜色饱和度适中，不刺眼
- 在白色和浅色背景上清晰可见

### 布局设计
- **卡片式设计** - 信息分组清晰
- **留白充足** - 视觉不拥挤
- **圆角边框** - 柔和友好
- **阴影效果** - 层次分明

## 📊 技术实现

### 数据处理
```javascript
// CSV数据读取和解析
fetch('./btc-price.csv')
  .then(response => response.text())
  .then(csvText => {
    const lines = csvText.split('\n').filter(line => line.trim());
    const processedData = lines.slice(1).map(line => {
      const [date, price] = line.split(',');
      return {
        date: date.trim(),
        price: parseFloat(price.trim())
      };
    }).reverse();
    setData(processedData);
  });
```

### 均线计算
```javascript
// 简单移动平均线算法
const calculateMA = (dayCount) => {
  const result = [];
  for (let i = 0; i < data.length; i++) {
    if (i < dayCount - 1) {
      result.push('-');
      continue;
    }
    let sum = 0;
    for (let j = 0; j < dayCount; j++) {
      sum += data[i - j].price;
    }
    result.push((sum / dayCount).toFixed(2));
  }
  return result;
};
```

### AHR999 计算
```javascript
// 200日定投成本：几何平均（对数前缀和 O(1) 查询）
const geoMean = Math.exp((logCum[i + 1] - logCum[i + 1 - 200]) / 200);
// 指数增长估值：币龄（自 2009-01-03 创世区块）的对数拟合
const coinAgeDays = /* 数据日期 - 创世区块日，单位天 */;
const fitted = Math.pow(10, 5.84 * Math.log10(coinAgeDays) - 17.01);
// AHR999 = 两个比值之积
const ahr999 = (price / geoMean) * (price / fitted);
```

### 性能优化
- **前缀和** - 均线与几何平均均为 O(1) 区间查询，一次遍历完成全部计算
- **useMemo** - 派生数据（均线、AHR999、回测、区间统计）统一缓存
- **Canvas渲染** - 使用Canvas而非SVG，性能更好
- **状态管理** - 合理使用React状态，避免不必要的重渲染

## 🔍 使用技巧

### 趋势分析
1. **多头市场识别**
   - 短期均线在长期均线上方
   - 均线呈现多头排列（MA50 > MA200 > MA350 > ...）
   - 价格在主要均线上方

2. **空头市场识别**
   - 短期均线在长期均线下方
   - 均线呈现空头排列
   - 价格在主要均线下方

3. **关键位置**
   - MA200被视为牛熊分界线
   - MA700是长期支撑/压力位
   - MA1400对应比特币4年减半周期

### 信号识别
- **金叉** - MA50上穿MA200，可能是买入信号
- **死叉** - MA50下穿MA200，可能是卖出信号
- **均线密集** - 多条均线靠近，可能预示突破
- **均线发散** - 趋势确立，可能延续

## 💡 最佳实践

### 数据更新
建议定期更新CSV数据文件，保持数据时效性：
```bash
cd scripts
python update_btc_price.py
```

### 自定义均线
根据自己的交易策略调整均线周期：
```javascript
// 在 src/App.jsx 中修改
const MA_DAYS = [50, 200, 350, 700, 1000, 1400];
```

### 浏览器兼容性
- 推荐使用Chrome、Firefox、Safari最新版本
- 支持移动端浏览器
- 需要启用JavaScript

## 🚀 未来规划

可能的功能扩展方向：
- [ ] 添加更多技术指标（RSI、MACD、布林带等）
- [ ] 支持多币种切换
- [ ] 添加价格预警功能
- [ ] 数据导出功能
- [ ] 暗黑模式支持
- [ ] 多语言支持
- [ ] 实时价格更新（WebSocket）
- [ ] K线图表模式

---

**注意**: 本工具仅用于技术分析学习，不构成投资建议。
